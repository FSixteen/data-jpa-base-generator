package io.github.fsixteen.data.jpa.base.generator.constants;

/**
 * 删除方式.
 *
 * @author FSixteen
 * @since 1.0.0
 */
public enum DeleteType {
    /**
     * 软删除(Soft Delete).
     */
    SOFT,
    /**
     * 硬删除(Hard Delete).
     */
    HARD;
}
